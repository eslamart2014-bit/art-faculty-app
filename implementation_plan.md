# خطة تنفيذ "محرك التكويد الذكي والمزامنة الشاملة"

هذه الخطة تغطي طلبك بالكامل لتحويل النظام إلى نظام ذكي يعتمد على الأسماء ويدير الأكواد والأرشيف تلقائياً.

## 1. تعديلات قاعدة البيانات (Database Updates)
سنقوم بإضافة الخصائص التالية لجدول الطلاب (`students`):
- `is_active` (boolean): لتحديد ما إذا كان الطالب مستمراً أم خريجاً/مؤرشفاً. الافتراضي `true`.
- `normalized_name` (text): اسم الطالب بعد معالجته (إزالة الهمزات والمسافات الزائدة والتاء المربوطة) لضمان دقة البحث وعدم تكرار الطالب بسبب خطأ إملائي بسيط.

## 2. محرك المزامنة الذكي (The Sync Engine)
سيتم تعديل زر "فحص وتحديث البيانات" ليعمل بالخطوات التالية:
1. **قراءة الشيت:** سحب (الاسم، الفرقة، السكشن) من جوجل شيت **(بدون عمود الكود)**.
2. **المطابقة بالاسم (Name Matching):** سيقوم النظام بمعالجة كل اسم في الشيت ومقارنته بالأسماء "النشطة" في قاعدة البيانات.
   - **الطلاب المستمرون:** إذا طابق الاسم، سيحتفظ الطالب بكوده القديم، ويحدث النظام فقط (الفرقة والسكشن).
   - **الطلاب الجدد:** إذا لم يطابق الاسم، سيقوم النظام تلقائياً بالبحث عن أعلى كود في قاعدة البيانات (مثلاً 0250) ويولد كوداً تسلسلياً جديداً لهذا الطالب (0251) وهكذا.
3. **التجهيز للأرشيف:** سيقوم النظام بحصر الطلاب "النشطين" في قاعدة البيانات والذين **لم يأتِ اسمهم في الشيت المرفوع** (مما يعني أنهم تخرجوا أو نقلوا).
4. **شاشة المراجعة (Preview):** قبل الحفظ النهائي، ستظهر لك شاشة تقول:
   - 🟢 سيتم إضافة (X) طالب جديد بأكواد جديدة.
   - 🔵 سيتم تحديث فرقة وسكشن (Y) طالب مستمر.
   - 🔴 سيتم تحويل (Z) طالب إلى أرشيف الخريجين.
5. **التأكيد والتسجيل:** عند ضغط "تأكيد"، ينفذ النظام العمليات ويسجل في الأسفل تاريخ العملية ونتيجتها (مثال: "تمت مزامنة 500 طالب بنجاح يوم كذا").

## 3. قسم أرشيف الخريجين (Graduates Archive)
- صفحة جديدة أو قسم في الإعدادات يعرض الطلاب المؤرشفين (`is_active = false`).
- سيتوفر زر **"تحميل بيانات الأرشيف"** يتيح تحميل كافة بيانات الخريجين (درجاتهم، حضورهم) كنسخة احتياطية للإدارة.

## 4. نظام تصدير الكشوف (Export Engine)
قسم جديد في لوحة الإدارة لتحميل الكشوف بعد المزامنة:
1. **استخراج PDF منظم للطلاب:**
   - تختار الفرقة (مثلاً: الرابعة).
   - يقوم النظام بتوليد ملف PDF احترافي مقسم حسب "السكاشن".
   - يضم جدولاً أنيقاً (م | اسم الطالب | الكود السري | السكشن).
   - يمكنك إرساله للطلاب لمعرفة أكوادهم الجديدة والسكاشن.
2. **استخراج Excel (شيت الكارنيهات):**
   - تحميل شيت إكسيل نظيف يحتوي على الاسم والكود فقط لترسله للمطبعة لطباعة الـ QR Codes.

## ⚠️ أسئلة للمراجعة (Open Questions):
1. هل صيغة الكود ستكون دائماً 4 أرقام مكملة بالأصفار؟ (مثلاً إذا كان الكود 15، يُكتب 0015).
2. هل تريد أن يبدأ التكويد للطلاب الجدد من `0001` (ويبحث عن الأكواد الناقصة) أم يكمل دائماً بعد أعلى كود مسجل في قاعدة البيانات لضمان عدم الرجوع للخلف؟ (مثلاً أعلى كود 0800، الجديد يكون 0801 مباشرة).

يرجى مراجعة الخطة وإخباري برأيك لتأكيد البدء!
