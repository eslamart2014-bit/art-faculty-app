# خطة تنفيذ "وضع الصيانة" وإشعارات الدفع (Push Notifications)

## 1. وضع الصيانة (Maintenance Mode) في الوقت الفعلي (Realtime)
سنقوم ببرمجة نظام صيانة فوري يعمل كالتالي:
- إضافة زر في "الإعدادات المتقدمة" للأدمن باسم **"تفعيل وضع الصيانة"**.
- عند تفعيله، سيتم تغيير حالة في قاعدة البيانات (Supabase).
- جميع المستخدمين الفاتحين للتطبيق حالياً سيتم طردهم فوراً (بفضل ميزة Supabase Realtime) وستظهر لهم شاشة تغطي التطبيق بالكامل مكتوب عليها "التطبيق تحت الصيانة الان، يرجى الانتظار...".
- عند تعطيل وضع الصيانة من قبل الأدمن، ستختفي الشاشة تلقائياً عند جميع المستخدمين ويتم عمل تحديث إجباري (Reload) لصفحاتهم لضمان حصولهم على آخر التحديثات.

**المطلوب منك (كأدمن):**
سنحتاج فقط لتشغيل كود SQL بسيط جداً في Supabase لإضافة عمود `is_maintenance_mode` في جدول الإعدادات.

## 2. نظام الإشعارات الخارجية (Push Notifications)
طلبك بأن يصل إشعار للهاتف **"من برا خالص بدون ما يكون فاتح التطبيق"** هو ميزة متقدمة جداً تُسمى (Web Push Notifications).
لتنفيذها نحتاج إلى بناء بنية تحتية كاملة في التطبيق:
1. **إنشاء مفاتيح تشفير (VAPID Keys):** مفاتيح خاصة لضمان أمان الإشعارات من سيرفرك لهواتف المستخدمين.
2. **جدول الاشتراكات (Subscriptions Table):** عندما يفتح أي طالب أو دكتور التطبيق، سيطلب منه "السماح بالإشعارات". إذا وافق، يتم تخزين "معرف هاتفه" في قاعدة البيانات.
3. **تطوير الـ Service Worker:** لكي يستقبل الهاتف الإشعار حتى لو كان التطبيق مغلقاً.
4. **زر إرسال الإشعار للأدمن:** عندما تنهي الصيانة، تضغط على زر "إرسال إشعار للجميع"، فيقوم السيرفر بإرسال رسالة لكل الهواتف المسجلة.

> [!WARNING]
> **ملاحظة حول أجهزة الآيفون (iOS):** إشعارات الـ Web Push تعمل على الآيفون فقط إذا كان إصدار النظام (iOS 16.4) فما أحدث، **وبشرط** أن يكون المستخدم قد أضاف التطبيق إلى "الشاشة الرئيسية" (Add to Home Screen). أجهزة الأندرويد تعمل عليها الميزة بدون مشاكل.

## هل نبدأ التنفيذ؟
إذا كانت الخطة مناسبة، **أخبرني لأعطيك كود الـ SQL الخاص بوضع الصيانة لنبدأ به أولاً**، ثم ننتقل لبرمجة الإشعارات خطوة بخطوة.
